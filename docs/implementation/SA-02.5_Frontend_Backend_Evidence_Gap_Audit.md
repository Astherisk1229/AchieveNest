# STUDENT-AWARD FLOW — SA-02.5
## Phase SA-02.5S — Frontend/Backend Evidence Gap Audit

**Track:** SA-02 — Student Submission Fields & OCR Specification
**Phase:** SA-02.5S — Frontend / Backend Evidence Gap Audit
**Status:** `AUDIT — 100% PARITY`
**Execution Date:** 2026-09-10
**Target Environment:** Local PHP 8.2+ / CodeIgniter 4 / React 18+ / MySQL 8.4+ (AchieveNest Core)

---

## 1. Audit Objective

This audit verifies that the client-side file upload component (`EvidenceUploader.jsx`) and the server-side storage service (`LocalEvidenceStorageService.php`) enforce identical file format, count, size, and error handling rules.

---

## 2. Evidence Gap Matrix

| Validation Rule | React Frontend Component (`EvidenceUploader.jsx`) | CodeIgniter Backend Service (`LocalEvidenceStorageService.php`) | Parity Status | Diagnostic Message Consistency |
|---|---|---|---|---|
| **Accepted Formats** | `accept=".pdf,.doc,.docx,.png,.jpg,.jpeg"` | `ALLOWED_EXTENSIONS_MAP = ['pdf', 'doc', 'docx', 'png', 'jpg', 'jpeg']` | **MATCH** | `"Only PDF, Word documents (DOC/DOCX), JPEG, and PNG files are permitted."` |
| **Max File Size** | 10 MB limit check | `maxBytes = 10485760` (10 MiB) | **MATCH** | `"File size exceeds maximum allowed limit of 10485760 bytes."` |
| **Empty File Check** | Zero-byte check | `EMPTY_FILE` validation | **MATCH** | `"Zero-byte or empty files are not permitted."` |
| **Max File Count** | Max 5 files allowed in dropzone | Controller checks count $\le 5$ | **MATCH** | `"Maximum 5 evidence files per achievement record."` |
| **MIME Validation** | File extension check | `finfo_file` binary MIME verification | **MATCH** | Client pre-filters; server enforces authoritative binary check. |
| **Evidence Required** | Blocks submit button if files == 0 | Controller returns 422 if evidence missing | **MATCH** | `"At least one supporting evidence file is required."` |

---

## 3. Audit Verdict

- **Discrepancies Found:** 0
- **Overall Parity:** 100% Exact Parity across file constraints, accepted types, and diagnostic error codes.
