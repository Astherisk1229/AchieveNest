# SQL Integrity Checks

No read-only integrity query was run because the configured target is the protected `achievenest_local` database. Required duplicate/orphan/invalid-code checks remain blocked pending a disposable K4 database.
