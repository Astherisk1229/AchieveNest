# Route and Endpoint Reconciliation

All requested Personnel Evaluation examples were found in `backend/app/Config/Routes.php`, using CodeIgniter `(:segment)` placeholders. The API map was rebuilt to 174 unique non-OPTIONS route/method pairs. Four identical Organization route/method declarations remain duplicated in `Routes.php`; they are represented once in the CSV and remain an unresolved production-source issue outside this documentation-only scope.
