# STUDENT-AWARD FLOW — SA-02.2
## Phase SA-02.2A — Requirement Source Audit

**Track:** SA-02 — Student Submission Fields & OCR Specification
**Phase:** SA-02.2A — Requirement Source Audit
**Status:** `AUDIT — VERIFIED`
**Execution Date:** 2026-09-10
**Target Environment:** Local PHP 8.2+ / CodeIgniter 4 / React 18+ / MySQL 8.4+ (AchieveNest Core)

---

## 1. Audit Objective

The objective of **Phase SA-02.2A** is to inspect and document the exact code and schema locations that enforce field requiredness across backend request validation, database integrity guards, metadata validators, and client-side form registries.

---

## 2. Requirement Source Matrix

| Category Domain | Field Code | Backend Requirement Source | Database Schema Nullability | Frontend Form Validation | Authoritative Rule Classification |
|---|---|---|---|---|---|
| **Base Record** | `title` | `StudentPortfolioController::create()` (Checks `$title !== ''`) | `NOT NULL` | `SharedAchievementFields.jsx` (`required: true`) | `REQUIRED` |
| **Base Record** | `category_id` | `StudentPortfolioController::create()` (Checks `$categoryId !== ''`) | `NOT NULL` (FK) | `SharedAchievementFields.jsx` (`required: true`) | `REQUIRED` |
| **Base Record** | `subcategory_id` | `PortfolioStructuredMetadataValidator::validateTaxonomyPair()` | `NULL` allowed at DB, required for structured sub-schemas | `SharedAchievementFields.jsx` (`required: true`) | `REQUIRED` |
| **Academic Term** | `academic_year` | `PortfolioStructuredMetadataValidator::validateMetadata()` (`$submitNow`) | Stored in JSON `structured_metadata` | `AcademicTermFields.jsx` (`required: true`) | `REQUIRED` |
| **Academic Term** | `semester` | `PortfolioStructuredMetadataValidator::validateMetadata()` (`$submitNow`) | Stored in JSON `structured_metadata` | `AcademicTermFields.jsx` (`required: true`) | `REQUIRED` |
| **Leadership** | `position_level` | `PortfolioStructuredMetadataValidator::CONTROLLED_VOCABULARIES` | Stored in JSON `structured_metadata` | `portfolioFormSchemaRegistry.js` (`required: true`) | `REQUIRED` |
| **Community Service**| `service_type` | `PortfolioStructuredMetadataValidator::CONTROLLED_VOCABULARIES` | Stored in JSON `structured_metadata` | `portfolioFormSchemaRegistry.js` (`required: true`) | `REQUIRED` |
| **Church Ministry** | `ministry_context`| `PortfolioStructuredMetadataValidator::CONTROLLED_VOCABULARIES` | Stored in JSON `structured_metadata` | `portfolioFormSchemaRegistry.js` (`required: true`) | `REQUIRED` |
| **Seminar / Training**| `training_type`| `PortfolioStructuredMetadataValidator::CONTROLLED_VOCABULARIES` | Stored in JSON `structured_metadata` | `portfolioFormSchemaRegistry.js` (`required: true`) | `REQUIRED` |
| **Sports** | `placement` | `PortfolioStructuredMetadataValidator::CONTROLLED_VOCABULARIES` | Stored in JSON `structured_metadata` | `portfolioFormSchemaRegistry.js` (`required: true`) | `REQUIRED` |
| **Journalism** | `publication_status`| `PortfolioStructuredMetadataValidator::CONTROLLED_VOCABULARIES` | Stored in JSON `structured_metadata` | `portfolioFormSchemaRegistry.js` (`required: true`) | `REQUIRED` |
| **Evidence** | `supporting_evidence` | `LocalEvidenceStorageService::validateEvidence()` | `student_portfolio_evidence` table | `EvidenceUploader.jsx` (`required: true` on submit) | `REQUIRED` |

---

## 3. Audit Conclusion

All requiredness rules are enforced multi-tier: in frontend registries for UX immediacy, in backend controllers and metadata validators for API integrity, and in database tables for physical data safety.
