# Phase 7 — OSAD Header Before / After Matrix
## Transition Matrix from Phase 6 Baseline to Phase 7 Standardized Header Anatomy

| Page / Sub-View | Phase 6 Header State | Phase 7 Standardized Header State | Primary Action Placement | Secondary / Filter Separation | Standardization Outcome |
|---|---|---|---|---|---|
| **OSAD Dashboard / Overview** | Custom inline flex banner with H1 | `OSADPageHeader` with single H1, eyebrow badge, responsive layout | Preserved 0 primary | Operational metrics in KPI grid below | Clean standardized header |
| **Academic Structure** | Inline flex box with H1 and action buttons | `OSADPageHeader` with icon, title, description, primary & secondary action slots | `Create College` (Right action zone) | `Create Academic Program` (with disabled tooltip) | Unified design token spacing & accessibility |
| **Student Accounts** | Custom flex container with `<h2>` tag | `OSADPageHeader` with semantic `<h1>`, icon, description, and primary action | `Add Student Account` (Right action zone) | Sub-tabs and multi-field filters remain in content toolbar | Semantic heading upgrade (`h2` -> `h1`) |
| **Student Organizations** | Custom flex banner with badge | `OSADPageHeader` with icon, title, badge, description, and primary action | `Create Student Organization` (Right action zone) | Scope & category filter pills in content toolbar | Standardized layout tokens |
| **Password Resets** | Custom flex banner with badge & refresh button | `OSADPageHeader` with icon, title, badge, description, and secondary action | 0 primary (Utility page) | Status filters and search bar in content toolbar | Clean action classification |
| **Awards & Scoring Criteria** | Custom banner with search input | `OSADPageHeader` with icon, title, badge, description, and search input | 0 primary (Catalog overview) | Accordion card filters & list below | Unified header tokens |
| **Award Candidate Review** | Custom flex banner with `CandidateReviewActions` | `OSADPageHeader` with icon, title, badge, description, and action zone | `CandidateReviewActions` (Right action zone) | Category tabs and college filter toolbar in body | Semantic and layout alignment |
| **Certificate Templates** | Custom banner with CTA | `OSADPageHeader` with icon, title, description, and primary CTA | `Create Certificate Template` (Right action zone) | KPI strip and context filter tabs in content | Clean token and spacing alignment |
| **Accreditation Reports** | Custom container with `<h2>` tag | `OSADPageHeader` with semantic `<h1>`, icon, and description | 0 primary | Print/Save as PDF actions remain inside individual report cards | Semantic heading upgrade (`h2` -> `h1`) |
| **OSAD Activity Log** | Custom flex container with `<h2>` tag | `OSADPageHeader` with semantic `<h1>`, icon, description, and utility CTA | 0 primary | `Refresh Activity Log` in secondary action slot | Semantic heading upgrade (`h2` -> `h1`) |
| **Coordinator Manager Subview** | Duplicate back buttons (left icon + right button) | `OSADPageHeader` (variant="detail") with semantic breadcrumb nav & left back control | 0 primary | Personnel assignment actions remain in data table | Zero duplicate back buttons |
| **College Details Subview** | Custom breadcrumb & action bar | `OSADPageHeader` (variant="detail") with semantic breadcrumb nav & action zone | `Add Academic Program` (Right action zone) | `Manage Program Coordinators`, `Edit College` (Secondary) | Standardized breadcrumb nav (`<nav aria-label="Breadcrumb">`) |
| **Organization Details Subview** | Custom breadcrumb & action bar | `OSADPageHeader` (variant="detail") with semantic breadcrumb nav & action zone | `Assign / Reassign Moderator` (Right action zone) | `Edit Details` (Secondary) | Standardized breadcrumb nav (`<nav aria-label="Breadcrumb">`) |
