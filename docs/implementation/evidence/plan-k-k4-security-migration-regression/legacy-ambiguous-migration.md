# Legacy Ambiguous Migration

The fixture is unresolved, but production `PersonnelClassificationService` has legacy fallback behavior that can normalize/derive canonical values without placement evidence. **BLOCKING DEFECT.**
