# Subject-to-Evaluation Fixture

- **State A (Active)**: `subject_to_evaluation = true` -> Evaluation workflow starts.
- **State B (Inactive)**: `subject_to_evaluation = false` -> Evaluation blocked at gate.
