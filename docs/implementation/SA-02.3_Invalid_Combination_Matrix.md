# STUDENT-AWARD FLOW — SA-02.3
## Phase SA-02.3L — Invalid Combination Matrix

**Track:** SA-02 — Student Submission Fields & OCR Specification
**Phase:** SA-02.3L — Invalid Combination Matrix
**Status:** `COMBINATIONS — ENFORCED`
**Execution Date:** 2026-09-10
**Target Environment:** Local PHP 8.2+ / CodeIgniter 4 / React 18+ / MySQL 8.4+ (AchieveNest Core)

---

## 1. Matrix Purpose

This matrix identifies individually valid controlled vocabulary values that are mutually exclusive or invalid when combined together within a single submission record.

---

## 2. Invalid Combination Rules Table

| Field A | Value A | Field B | Value B | Combination Status | Sanitization / Validation Action |
|---|---|---|---|---|---|
| `individual_team` | `'individual'` | `team_role` | `[non-empty string]` | **INVALID** | `team_role` is purged automatically during backend sanitization. |
| `publication_status`| `'draft'` | `publication_date` | `[valid date]` | **INVALID** | `publication_date` is purged automatically during backend sanitization. |
| `performance_type` | `'solo_performance'` | `individual_group` | `'group'` | **INVALID** | Rejected by validator: solo performance cannot have group structure. |
| `position_level` | `'executive'` | `committee_name` | `[non-empty string]` | **INCOMPATIBLE** | Ignored by scoring engine; executive level evaluates whole-org scope. |
| `publication_type` | `'editorial'` | `authorship_role` | `'illustrator_photographer'`| **INCOMPATIBLE** | Replaced by `lead_author` or rejected if editorial text piece. |

---

## 3. Enforcement Mechanism

1. **Frontend**: Dynamic forms hide and reset dependent fields as soon as trigger values change.
2. **Backend**: `PortfolioStructuredMetadataValidator::validateMetadata()` unsets incompatible keys (e.g. Lines 220-226) before persisting into `structured_metadata`.
